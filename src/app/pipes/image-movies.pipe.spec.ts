import { ImageMoviesPipe } from './image-movies.pipe';

describe('ImageMoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageMoviesPipe();
    expect(pipe).toBeTruthy();
  });
});
